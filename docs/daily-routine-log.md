# Daily routine — log

Each day Claude appends one entry per run of `docs/daily-routine.md`. Newest at the bottom. Use this to spot patterns: same bug class recurring across days, drift in volume, recurring escalations.

Keep entries tight (~10 lines). When the file gets long, archive the oldest months into a yearly file (`daily-routine-log-2026.md`).

> History note: this file was named `docs/morning-prompt-log.md` before 2026-05-27. Entries from before that date still use the old framing ("morning routine") — the content is unchanged.

---

## 2026-05-21 (~60min)

- Overnight: curator surfaced 7 events for 2026-05-21 (SUPERMOON 9th anniv, 2× Kintsugi @ Dragon Tea Temple, Full/New Moon kirtans, Master Lover couples retreat, Heart Space Bali cacao). Maintenance digest flagged 9 missing-`start_time` events + 2 broken links. Approver disabled, queue was sitting unprocessed in `curator/inbox/`.
- Approver work: approved=13, off_topic=10, dup=2, repaired=2, escalated=0. **Root cause for the empty pending queue: GH workflow `curator-ingest.yml` used `ls -t` on a fresh checkout — equal mtimes → unstable sort → it kept POSTing 2026-05-19.json (the oldest file) for three days. Fixed (use `sort | tail -1`) and manually `curl`-POSTed the 2026-05-20 + 2026-05-21 inboxes; both reported 7/7/0/0 ingested.** Digest cleanups: 7× training-not-gathering (Inner Temple ×3, Momentum Contact Facilitator Training ×3, Anandasarita Inner Mastery), 1× dead-link (Divine Darkness "Sunday Worship"), 1× not-specific (Regular Circling), 1× off-brand commercial (Heart Space Bali — confirmed via web fetch, this is destination-wellness tourist-coded, not local ceremony). Dedup: Songs of the Dragonfly ingested twice w/ same Megatix URL (kept long-titled variant), Cremoso Night zouk has Mon+Tue duplicates from conflicting day_of_week parses (kept the one with the WhatsApp join link).
- Spot-check: Card↔detail parity OK on SUPERMOON + Kintsugi Jun 26 (post-rename). No past-date leakage. Title bug surfaced: `/experiences` rendered "Ubud Retreats | The Ubudian | The Ubudian" — root layout uses template `%s | The Ubudian` but ~30 page metadata files redundantly included the suffix. Stripped via sed across `src/app/**/page.tsx`.
- Site cross-check: all 10 main routes 200. `/membership` had no metadata of its own (rendered tab title "The Ubudian"); added one. Yesterday's followup remains: membership tier copy still promises "Humans of Ubud stories" while `stories_enabled=false`.
- Committed: migration `20260521082535_morning_triage_2026_05_21.sql` (14 triage rows + 2 dedup rows); workflow fix in `curator-ingest.yml`; sed pass stripping ` | The Ubudian` from 30+ page metadata files; `/membership` metadata.
- Followups: (1) Promote Dragon Tea Temple to `priority_b` in `curator/sources.json` — 3 of today's approvals came from there; the curator's Sunday Step-11 promotion logic should already catch it but worth a manual nudge. Heart Space Bali should be moved to `rejected_permanent` so it stops re-surfacing. (2) Several long-running approved events have no cover image (Songs of Dragonfly, Solo Improv w/ Nastya, Authentic Relating Games, Thai Massage Jam) — cards fall back to gradient placeholders. (3) Newsletter signup not surfaced on `/events` where intent is highest. (4) The `ls -t` class of bug could exist in the approver-fetch + approver-apply workflows too — audit when they're next active.
- Prompt-evolution: proposed an edit to step 1 of the prompt — see wrap-up.

## 2026-05-20 (~60min)

- Overnight: approver self-healed the BLOCKED stub from 2026-05-19; approved 5 / off_topic 1 / dup 0 / escalated 0. Curator surfaced 7 new events, 4 venues (Dragon Tea Temple, Azadi, Bara Studio, AYMC), 6 facilitators.
- Approver work: pending queue was empty — approver had drained it. My contribution: archived 191 stale `approved` events whose start_date had passed (oldest 2026-03-09), plus 3 dead-link 404s (megatix typo slugs + dead embodiedawakeningacademy URL).
- Spot-check: 8 events (Beauty Way, Full Moon Raio, Friday ED Yoga Barn, Vibración, Dragonfly, Entropic, Contact Dojo Jam, Solo Improv w/ Nastya). Card↔detail parity intact; Entropic shows 11am Wed contact-dance (worth a source check); Vibración price "IDR 90000" no commas. Friday ED megatix slug really is 404 (slug `friday-ecstatic-dance-25` rotates) — cleared the URL.
- Site cross-check: /events + / hero subtitles + CTAs were invisible (charcoal text on dark bg). Root cause: `--brand-cream` and `--brand-deep-green` invert in `.dark`, but those heroes lock their bg dark in both themes. Fixed both heroes with literal hex. /guides, /experiences, /membership healthy. /stories 404 is intentional (`stories_enabled=false`) but membership tier copy still promises "Humans of Ubud stories" — copy bug.
- Committed: migration `20260520070000_archive_stale_approved_events.sql`; `archivePastApprovedEvents()` wired into nightly maintenance; `checkExternalLinkHealth()` uses real browser UA + HEAD→GET fallback; hero text fixes on `events-hero.tsx` + `app/page.tsx`; copy "on right now" → "on the agenda right now".
- Followups: (1) Entropic 11am — sanity-check at source. (2) Membership copy promises stories while stories_enabled=false. (3) 359 usages of `text-brand-cream`/`text-brand-deep-green` — audit any other locked-dark section. (4) Yoga Barn Friday ED needs a per-week URL refresh strategy.
- Prompt-evolution: yes — add a brand-register note about `--brand-cream` inversion on locked-dark heroes (proposed in the wrap-up reply).

## 2026-05-25 (~55min)

- Overnight: maintenance digest healthy (1 archived past pending, 4 broken links, 0 dedup backlog). Curator surfaced 14 candidates → 2 to inbox (Bara Studio Essence of Love + Heart Space Bali Fri Cacao, both already in DB or filtered) and PROMOTED Heart Space Bali to priority_b. Pending queue had 10 events accumulated 2026-05-21..2026-05-23.
- Approver work: approved=2 (Akasha Sat ED, Moving Waves 5Rhythms Tue) + repaired=2 (Pyramids Full Moon ceremony 18:00 + monthly; Ecstatic Dance in the Dark → weekly Wed) + off_topic=8 (4 stale Megatix listings + later rolled back 4 more I'd briefly approved). Cleared the friday-ecstatic-dance-25 megatix URL (slug rotates weekly, dead each time).
- Spot-check: **discovered a systemic curator bug** — it's year-rolling stale Megatix slugs (events that ended in 2021, 2023, 2024, 2025 surfaced as future 2026 dates). 6 events archived for this reason this morning, including SUPERMOON Jun 14 which had been approved since the original ingest (megatix-suara-semesta-001 is the 2025 edition; new editions get -002, etc.). AYNI Jun 14, New Moon Cacao Jun 12 (Levi Banner), Ecstatic Dance in the Dark all same root cause. Added a hard rule to `curator/playbook.md` 2026-05-25 lessons. Also caught a render bug: image-less event cards render the category twice (once in the editorial placeholder typography + once in the Badge chip). Fixed `event-card-placeholder.tsx` to keep just the hairline gold rule.
- Site cross-check: all 12 main routes 200. Live agenda (45 → now 41 approved after rollback) reads clean, "Featured today" pulled correct Monday event (Contact Dojo Jam 6PM at Moksa). Mobile hero shows live count "45 gatherings on the agenda right now" — needs a refresh now that we're at 41; that's a server-render cache issue, will resolve next ISR. New Akasha Sat event surfaces with "NEW" chip.
- Committed: migration `20260525071500_morning_triage_2026_05_25.sql` (10 pending decisions + 1 URL clear); migration `20260525083000_morning_triage_2026_05_25_rollback_stale_megatix.sql` (4 stale-megatix rollbacks); `event-card-placeholder.tsx` declutter; `event-hero.tsx` strip unused `text-6xl`; `curator/playbook.md` 2026-05-25 lesson on stale-Megatix year-rolling.
- Followups: (1) Curator agent doesn't enforce the new playbook rule programmatically — needs prompt update or a pre-inbox filter that does HEAD/GET on Megatix URLs and rejects pages containing "This event has already taken place". (2) Solo Improvisation w/ Nastya has NULL venue_name + free-text recurrence rule "Every Wednesday" — same drift class as the Pyramids of Chi event I just normalised. Worth a sweep: how many approved events have free-text recurrence_rule vs RRULE? (3) Akasha + Moving Waves were approved without external URLs from curator inference — both look credible but unverified; if next week's Akasha doesn't materialise, archive. (4) AYMC has 2 qualifying curator logs already; one more away from priority_a promotion. Worth surfacing the Sayan/Alchemy Rise Ecstatic Dance Tuesday 7-9pm as a manual add.
- Prompt-evolution: yes — one-line addition to "The work" step 2, see wrap-up reply.

## 2026-05-27 (~40min)

- Overnight: quiet run — maintenance digest clean (0 cleanups, 2 failed messages purged, 1 broken link: Bara Studio tickettailor 403 = same Cloudflare false-positive class flagged yesterday). Curator surfaced 8 candidates → 0 to inbox (all already-ingested weeklies, geo-out, or 403-blocked). Pending queue genuinely empty (verified via DB).
- Approver work: approved=0 off_topic=0 dup=0 repaired=0 escalated=0 (nothing in pending).
- Spot-check: 5 events (Authentic Relating Games, Friday ED Yoga Barn, Master Lover Retreat, Ilahi Sufi Qawwali, Dance of the Dragonfly Sunset). 4/5 card↔detail parity intact. **1/5 mis-dated: Friday Ecstatic Dance — The Yoga Barn renders "Saturday May 30" everywhere (card + detail + agenda).** Root cause: `nextOccurrence()` in `buckets.ts` (and mirror in `recurrence.ts` `expandRecurrence`) only ran the day-walker for `Array.isArray(rule.day_of_week)` — single-number `day_of_week` fell through to a `start_date + 7*N` stride. Friday ED has `recurrence_rule="FREQ=WEEKLY;BYDAY=FR"` parsed to `day_of_week:5` (number) but `start_date=2026-05-23` (Saturday) → stride landed on Saturdays. Same class affects future-anchored row "Dance of the Dragonfly Expanded" (rule WE, seed Thu Jul 23) once that date arrives. Fix: day-walker now runs for any weekly rule with `day_of_week` defined (number or array), anchored at `max(today, start_date)`. 548/548 unit tests pass; `npm run build` green.
- Site cross-check: 8 main routes 200 (/events, /, /experiences, /membership, /quiz, /guides, /about, /partners). Membership copy no longer references "Humans of Ubud / Stories" — yesterday's followup #2 is resolved. Mobile 390px /events hero renders cleanly (lush, readable, brand-tonal). 2× `<h1>` on detail pages persists (known E2E failure per MEMORY.md). Home "What's Happening" features Moving Waves (Tue) + Saturday ED + Full Moon Blue Moon Cacao + Master Lover — looks editorially curated rather than chronologically-today-first; intentional but worth a re-check (with my fix, Moving Waves now rolls to Tue Jun 2 — confirm).
- Discoveries: curator added Akari Tamura (Anandamoyi Dasi) as facilitator-pending (Dragon Tea Temple Wed Kirtan bi-weekly). 6th consecutive 403 on PURNAMA Megatix — full moon dates corrected to Jun 29 / Jul 29 in sources.json. AYMC Full Moon Bhakti Aug 8 surfaced as next confirmed.
- Committed: `<TBD>` fix(events): weekly recurrence day-walker honours single-number day_of_week — repairs Friday ED Saturday-misdating and any future row whose seed weekday disagrees with rule.
- Followups: (1) **link-health checker still false-positives on Cloudflare** — Bara Studio tickettailor 403 in today's digest is identical pattern to yesterday's, will keep landing daily until the checker differentiates `cf-mitigated` / "Just a moment..." from genuine 404. Highest-priority infra fix. (2) Three recurrence-rule formats still coexist (14 JSON, 9 RRULE, 2 normalised); a one-time migration to one canonical shape (JSON) would simplify the parser surface. (3) Home page "What's Happening in Ubud" featured-event ranking doesn't surface today's events at all — investigate whether by design or stale. (4) Curator's `playbook.md` stale-Megatix-detection rule is still not enforced programmatically (carried over from 2026-05-25).
- Prompt-evolution: yes — one-line addition to step 4 (spot-check), see wrap-up.

## 2026-05-28 (~50min)

- Overnight: maintenance digest clean (0 cleanups, 1 false-positive 403 = Bara Studio Cloudflare class persisting daily). Curator surfaced 8 candidates → 0 to inbox in the daily run (all weeklies / 403-blocked) BUT the same day's competitor-harvest sweep added 3 inbox items (Five Temples of Her, Shamanic Breathwork with Jane, Shakti Bhakti Kirtan). Pending queue had 14 events accumulated (megatix 12 + curator 2).
- Approver work: approved=6 off_topic=7 dup=1 repaired=4 (start_time backfills from description quotes) + 2 RESURRECTED archived-recurring rows (Shamanic Breathwork @ Yoga Barn Wed; Shakti Bhakti Kirtan @ Intuitive Flow Wed). **Discovered a code bug**: `archivePastPendingEvents()` in `src/lib/ingestion/alerts.ts` archives ANY pending row with `start_date < today`, missing the `is_recurring=false` guard that its sibling `archivePastApprovedEvents()` has. When the competitor-harvest sweep landed recurring rows with start_date=today's-UTC-date as pending, the next maintenance cron wrongly archived them. One-line fix shipped: `.eq("is_recurring", false)` added. 548/548 tests still pass.
- Feedback: actioned=0 dismissed=0 reviewed=1 — Russ (russty75@gmail.com) on /quiz: praises quiz UX, asks to list his Wed-eve Authentic Relating event. The existing approved AR Games row is hosted by Ram, so Russ runs a different Wed AR series — flagged for Benji to reply with trusted-submitter pathway + intake fields. Also: Russ suggests a "healing" archetype for the quiz — quiz-v2 candidate.
- Spot-check: 6 newly-approved detail pages all 200; 5 Megatix external links all 200; Shamanic Breathwork detail page renders correctly with day-walker rolling May 27 (Wed seed) → June 3 (next Wed) — recurrence fix from 2026-05-27 still holding. Card↔detail parity intact. Hero count "49 gatherings on the agenda right now" matches DB.
- Site cross-check: all 10 main public routes 200. Mobile 390px /events hero renders lush + brand-tonal (no `text-brand-cream` inversion regressions). Detail page cross-link section "More Ceremony & Sound Events" working — Shamanic Breathwork detail shows related kirtan/sound events. /stories returns 200 (placeholder rendering, stories_enabled=false).
- Discoveries: curator log surfaced 3 venues (ASH at Nuanu, La Portal to Shamballah, Intuitive Flow Yogastudio) and 2 facilitators (Mariama Del Mar, Anu Karoliina) on yesterday's competitor-harvest. Intuitive Flow weekly Wed kirtan is now live (resurrected); ASH at Nuanu still 1-of-3 qualifying logs. Akari Tamura (Dragon Tea Temple bi-weekly Wed Kirtan) added to facilitators_pending — track for Megatix unblock.
- Committed: `<TBD>` migration `20260528083000_daily_routine_2026_05_28.sql` (14 pending decisions + 2 resurrected recurring); `src/lib/ingestion/alerts.ts` one-line `is_recurring=false` guard; daily-routine-log entry. Single commit, single push.
- Followups: (1) **Artifact filing convention is UTC-dated, not WITA** — the daily-routine prompt's `$(TZ=Asia/Makassar date +%F)` looks for files dated 2026-05-28 when the actual artifacts land under 2026-05-27 (UTC at trigger fire time = 19:17 UTC May 27 = 03:17 WITA May 28). I figured it out from commit message + log timestamps but a future run might waste 5 minutes on this. **Prompt-evolution proposal below**. (2) Cloudflare false-positive on Bara Studio tickettailor 403 persists (carried from 2026-05-27, 2026-05-26 — 3 consecutive days). Highest-priority infra fix: differentiate `cf-mitigated` / "Just a moment..." from genuine 404 in `checkExternalLinkHealth`. (3) Three recurrence-rule formats still coexist in the DB (JSON, RRULE, freetext) — carryover. (4) The competitor-harvest sweep landing 3 useful events (vs. daily curator's 0) suggests Blissbase + Soulwise scouting is now the higher-value path on quiet days; consider promoting that walk to a daily slot, not just on-demand.
- Prompt-evolution: yes — see wrap-up.

## 2026-05-26 (~55min)

- Overnight: maintenance digest clean (0 cleanups, 2 broken-link 403s — both Cloudflare bot-block false-positives, NOT genuine deadness). Curator surfaced 3 candidates (Ilahi Sufi Qawwali Yoga Barn Jun 17, Heart Space Bali Sun cacao, Pyramids of Chi Full Moon Jun 29) → 2 dedup'd against existing rows → 1 landed in pending. Discovered Om Ham Retreat + Ilahi Sufi + Sawitri facilitators.
- Approver work: approved=1 (Ilahi Sufi Qawwali, Megatix URL verified 200) + archived=2 (Inner Temple Level 2 + Alchemy & Qigong Intensives — multi-day intensives that escaped `archivePastApprovedEvents` because `end_date>=today` for the longer one) + repaired=3 (Contact Dojo Jam Monday recurrence corruption + 2 freetext recurrence normalisations) + url_cleared=1 (megatix `ecstantric-dance` genuinely 403 even in browser).
- Spot-check: **discovered a render bug on the hero card.** "Featured today" badge was hard-coded; hero = ranked[0] regardless of when it happened. Yesterday (Mon) it surfaced Contact Dojo Jam Mon — coincidentally correct. Today (Tue) it surfaced Contact Dojo Jam with date "Wednesday, May 27" (next M/W/F instance via corrupted recurrence rule) under the same "Featured today" badge — triple lie: label/date/description all disagreed. Fixed at code: hero now walks bucketEventsByTime nearest-to-furthest, picks top-ranked event within first non-empty bucket, label adapts (Happening now / Featured today / Featured tomorrow / Featured this weekend / Featured this week / Coming up). Card↔detail parity verified across 4 events (Moving Waves, Contact Dojo Mon, Authentic Relating, Pyramids of Chi) — all match. Recurrence-rule schema audit: 14 JSON + 9 RRULE + 2 freetext approved recurring rows. Normalised the 2 freetext rows (Solo Improv w/ Nastya, Authentic Relating Games) to JSON; 9 RRULE rows still work via tolerant parser but should be normalised in a future migration.
- Site cross-check: all 10 main public routes 200. `/journeys` 404 but no nav points to it — false alarm. Mobile + desktop hero render verified post-deploy.
- Committed: `e8f00b2` fix(events): hero anchors to today's bucket, label adapts; SQL-only triage (approve Ilahi, archive 2 intensives, fix Contact Dojo Mon recurrence to [1], clear ecstantric URL, normalise 2 freetext rules) — direct UPDATEs, no migration since <10 rows.
- Followups: (1) **link-health checker false-positives on Cloudflare bot-blocked hosts** — tickettailor.com always 403 to UA-spoofed HEAD but the page is fine; megatix.co.id mostly works (200 on Ilahi, 403 on dead slugs). The check needs to differentiate genuine 404 from Cloudflare challenge (look for `cf-mitigated` header or "Just a moment..." in body) before flagging. Same noise will keep landing in the digest every day until fixed. (2) Three recurrence-rule formats coexist — write a one-time migration to normalise all 9 RRULE rows to JSON so the schema has one canonical shape. (3) `archivePastApprovedEvents` missed Inner Temple Level 2 (end_date 2026-05-25 < today 2026-05-26) — the SQL filter looks correct, may be a `nowInBali` edge or the cron didn't run; worth instrumenting one log line. (4) Yesterday's curator-playbook stale-Megatix rule is still not enforced programmatically — Ilahi link was 200 today, but the next Megatix import may year-roll something dead again.
- Prompt-evolution: yes — one-line addition to step 8, see wrap-up reply.

## 2026-05-31 (~50min, Sunday)

- Overnight: quiet, healthy "empty day" — curator ingested 0 events (pipeline caught up on the 60-day window; expected per `project_event_pipeline_overview`), discovered facilitator Kim Kindersley, and promoted AYMC → priority_a on its Sunday weekly-maintenance pass. Maintenance digest clean (0 archived, 2 failed msgs purged) but flagged 2 "broken" links + 39 low-confidence unresolved venues. No approver stub (approver disabled by design).
- Approver work: approved=1 off_topic=0 dup=1 repaired=2 (URL clears) escalated=0. The lone pending row "Shakti Bhakti: Kirtan & Cacao" (Mudra Cobra Room Thu) is **web-confirmed real** (Anu Karoliina, Thu 6–7:30pm) → approved (cleared its stale megatix CTA). Discovered the already-approved sibling "Shakti Bhakti Kirtan @ Intuitive Flow Wed" was a curator **venue/day mis-attribution** (Intuitive Flow is her flow-yoga venue, not a kirtan venue) → archived as dup. Cleared stale megatix CTA on the future (Jun 25) New Moon Kirtan @ Dragon Tea Temple. Also archived 4 concluded May 29/30 single-day events the nightly `archivePastApprovedEvents()` missed (it reported "0 archived" — transient throw caught as 0; same class as 2026-05-26 followup #3). These don't leak to users (in-memory nowInBali filter) but polluted admin/dedup. Approved total 49→45 = matches the live hero count exactly.
- Feedback: actioned=0 dismissed=0 reviewed=1 — Luca (luca-goldhorn@web.de) on /events: hero too large (wants events without scrolling), wants denser list w/ small/no images (cards crop), one "Today" section, no time-bucket subdivisions. Verified on mobile: hero is full 844px viewport → zero events above the fold. Marked `reviewed` + escalated as a design-direction call (conflicts with the intentional editorial hero). Most useful report this week — concrete, repeatable, mobile-first.
- **Code fixes (2)** for recurring bug classes: (1) `checkExternalLinkHealth` (`src/lib/maintenance/cleanups.ts`) now reads GET bodies and classifies Cloudflare challenges (403/503 + "Just a moment"/cf-mitigated) as HEALTHY (kills the 4+-day tickettailor false-positive — followups 2026-05-26/27/28), and flags megatix 200-but-"already taken place" pages as `stale` (the long-unenforced stale-Megatix detection, now programmatic). +2 unit tests, 550/550 pass. (2) Wired a redundant `archivePastApprovedEvents()` sweep into the `ingestion-health` cron (30 min after maintenance) so a single transient archival miss no longer leaves stale rows for a full day.
- Spot-check: 8 events via SQL integrity sweep + Mudra detail page. Mudra kirtan detail renders "Thursday, June 4" (correct next-Thu roll — recurrence day-walker fix still holding), no broken CTA, cross-links work. Past-date leakage: 4 found → archived → now 0. Recurrence-format drift audit: 30 recurring rows = 17 JSON / 12 bare-FREQ / 1 RRULE (carryover, works via tolerant parser).
- Site cross-check: all 10 public routes 200. Home (mobile) brand-tonal + alive ("What's Happening" featured Sunday Kirtan; "Curated paths" journeys; quiz; newsletter). /events hero full-viewport on mobile (Luca's point). No dark-mode inversion, no horizontal scroll.
- Discoveries: promoted **Mudra Cafe (The Cobra Room) → priority_b** (2 curator logs + today's web-confirmed live approval = 3rd signal). Corrected Anu Karoliina facilitator note (kirtan = Mudra Thu, not Intuitive Flow Wed). Kim Kindersley Dragonfly 2026 slug still pending (early July watch). ShambAllah WhatsApp invite still outstanding (day 11+ — Benji escalation, highest-value unjoined scout).
- Committed: migration `20260531030000_daily_routine_2026_05_31.sql` (approve Mudra + archive Intuitive-Flow dup + 2 URL clears + 4 past-event archival); `cleanups.ts` link-health classifier + 2 tests; `ingestion-health/route.ts` redundant approved-sweep; `curator/sources.json` Mudra promotion + Anu correction; feedback row reviewed; this log. Single commit, pushed.
- Followups: (1) **/events hero density** — Luca's feedback awaiting Benji's design call (recommend: cap hero ~70vh mobile so first card peeks). (2) Recurrence-format drift (17 JSON/12 FREQ/1 RRULE) — one-time normalisation migration still owed (carryover ×4). (3) 39 low-confidence unresolved venues — worth a batch resolve pass next quiet day. (4) Watch next maintenance digest: tickettailor should now drop OUT of broken-links and megatix stale slugs should appear as `stale` (validates today's link-health fix in prod).
- Prompt-evolution: none — the prompt held up well this run.

## 2026-06-01 (~60min, Monday)

- Overnight: quiet new-month run. Maintenance digest clean (0 archived past pending, 4 failed msgs purged, 0 dup) but flagged 40 low-confidence unresolved venues; **digest reported 0 broken links — which turned out to be a detection bug, not a healthy agenda** (see below). Curator walked 14 sources (all A-tier 403/egress-blocked in sandbox), surfaced 1 inbox event. Approver disabled by design (no stub — correct).
- Approver work: approved=0 off_topic=0 dup=0 **repaired=1 (CTA cleared)** **archived=10** escalated=1. The lone pending row "Inner Meeting" tantra retreat (Jul 12-14) was a **stale-Megatix year-roll** — its `inner-meeting` page is the Jul 12-14 **2024** edition ("already taken place"). Archived. Then archived "Full Moon Blue Moon Cacao" (May 31, past leak the nightly sweep missed again) + **8 more live-agenda phantoms** (see spot-check). Cleared CREMOSO's WhatsApp-invite CTA (forbidden-domain violation). Approved 44 → 36.
- Feedback: actioned=0 dismissed=0 reviewed=0 — `feedback WHERE status='new'` empty.
- **Systemic finding — the agenda was ~18% phantom Megatix events.** Every upcoming single-day Megatix-sourced event resolved to a PAST edition the curator year-rolled to 2026: Kintsugi Jun 5 (→2022), Kintsugi Jun 26 (→2022), Beauty Way Jun 29 (→2024), New Moon Bhakti Kirtan Jul 6 (→2024), Beauty Way Jul 8 (→2023), Ilahi Sufi Jun 17 (→2023, the "verified 200" row from 2026-05-26 — 200 ≠ current), Awakening the Heart Aug 23-24 (→2025), YUJOY Jun 8 (URL = Dec 2024, date unconfirmed). All archived; live agenda now reads 36 real events, 0 phantom titles on / or /events.
- **Root-cause code fix #1 (link-health).** Prod link-health reported `broken:[]` because `LINK_BODY_SCAN_BYTES=4000` but megatix's "already taken place" copy sits at byte ~26670 — the stale regex never saw it. Fixed `cleanups.ts`: parse the JSON-LD `startDate`/`endDate` (offset ~600, always in-window) and flag any past-dated event as `stale`, bumped scan window to 40KB as a text-marker fallback. +1 test (552 total).
- **Root-cause code fix #2 (recurrence).** "Sunday Kirtan with Vasudev" (rule `BYDAY=SU`, start_date seeded on Mon Jun 1 == today) rendered "Monday June 1" on card AND detail. The active-instance guard in `buckets.ts nextOccurrence` short-circuited on `start_date == today` before the weekday-snap ran. Fixed: guard now requires a genuine multi-day span (`end_date > start_date`); single-day recurring rows fall through to the weekday-snap → rolls to Sun Jun 7. +1 test.
- Spot-check: 8 Megatix URLs cross-checked against live JSON-LD dates (all phantom, see above) + Sunday Kirtan card↔detail (both wrong → code-fixed, not row-patched). Recurrence-format drift unchanged (mix of JSON/FREQ/RRULE).
- Site cross-check: all 10 public routes 200. Desktop /events hero readable (gold/cream on green, no inversion), count "36 gatherings" matches DB. Mobile /events hero still full-viewport (Luca's open design call). Homepage rails (What's Happening / journeys / quiz / newsletter) clean, no phantoms.
- Discoveries: none new (curator found 0 venues/facilitators). ShambAllah WhatsApp invite still outstanding (day 12+, highest-value unjoined scout — Benji escalation).
- Committed: migration `20260601012123_daily_routine_2026_06_01.sql` (Inner Meeting + past-leak archive); migration `20260601013000_archive_stale_megatix_phantoms_2026_06_01.sql` (8 phantom archives + CREMOSO CTA clear); `cleanups.ts` JSON-LD stale detection + test; `buckets.ts` recurrence guard fix + test; this log. Single commit, pushed.
- Followups: (1) **Auto-archive stale phantoms in nightly maintenance** — detection now works; recommend wiring stale-link → archive (non-recurring) / clear-CTA (recurring) so phantoms never reach prod. Awaiting Benji nod (auto-mutation from cron). (2) **Year-check at ingestion** — pipeline (Vercel, can reach megatix) should read JSON-LD startDate and reject past-dated no-year slugs at the source. The real fix. (3) Newsletter signup still absent on /events (carryover, highest growth lever). (4) Image-less backbone weeklies render gradient placeholders (carryover). (5) Recurrence-format drift normalisation (carryover ×5).
- Prompt-evolution: yes — see wrap-up (spot-check must verify Megatix YEAR, not just 200).

### 2026-06-01 addendum (~75min more — Benji: "verify EVERYTHING, ship them all")

- **Full link-verification deep-dive (not a sample).** Fetched every external_ticket_url + source_url on all approved events, checking HTTP status + JSON-LD date + stale markers. The morning's phantom finding was the tip: **most single-day Megatix listings were stale, and several recurring CTAs pointed at dead/2018/2022 editions.** Actions: cleared 3 dead recurring CTAs (Vibración=2018, Shamanic Breathwork, Full Moon Kirtan=2022); **repointed both Dance of the Dragonfly rows** to the live venue page (dragonfly-village.com) instead of the dead megatix slug; **repaired Momentum CI Workshop date** (DB said Jun 13-14, live page says Ubud edition is Jun 6-8 — users would've arrived a week late); **archived New Moon Kirtan Jun 25** (2022 source, Jun 25 isn't even the June new moon). Final sweep: **zero dead/stale CTAs remain** — every CTA verified current (tickettailor 403 = Cloudflare false-positive; "sold out" pages = real upcoming). Confirmed all 3 discovery festivals real 2026 (Open Studios Jun 5-7, Jazz Aug 7-8, Writers Oct 21-25).
- **CREMOSO WhatsApp CTA restored** (Benji: event-owned WhatsApp group is a fine CTA) + refined `curator/playbook.md`: forbidden = a channel we *scout from*; allowed = a host's *own* group for *their* event.
- **Shipped all 5 improvements:**
  1. **Auto-archive stale phantoms in nightly maintenance** (`cleanups.ts archiveStaleTicketedEvents` + wired into `daily-maintenance/route.ts`): non-recurring stale link → archive, recurring → clear CTA. Closes the loop so phantoms never live >1 day. +2 tests.
  2. **Year-check at ingestion** (`src/lib/events/ticket-freshness.ts` + `pipeline.ts`): for non-recurring stale-prone hosts, fetch JSON-LD date and reject if past — prevents phantoms at creation. Best-effort, 4s timeout. +6 tests.
  3. **Newsletter band on /events** (highest-intent surface) — editorial deep-green band, literal-hex cream.
  4. **6 AI cover images** for backbone weeklies (Contact Dojo Jam, Contact Class, Solo Improv, D-Xperience, 2 kirtans): Stability Core, editorial/NatGeo prompts, visually reviewed each before publishing — all on-brand (no hippie-slime). Festivals left for real photography.
- Tests 552 → **560**; build green. Approved now 39 (discovery festivals landed concurrently), pending 0, 0 dead CTAs, 6 fewer placeholder cards.
- Committed: migrations `..020000_link_health_repair`, `..021000_..part2`, `..030000_cover_images`; `cleanups.ts` + route (auto-archive); `ticket-freshness.ts` + `pipeline.ts` (ingest year-check); `events/page.tsx` (newsletter); `playbook.md` (WhatsApp rule); 3 test files. 
- Followups: (1) Festivals + ECSTANTRIC still placeholder — want real official photos, not AI (5 events). (2) Watch tomorrow's maintenance digest: stale-link auto-archive + JSON-LD detection should now show non-zero `archivedStaleLinkEvents`/`clearedStaleCtas` and flag any new stale slugs.

## 2026-06-02 (~50min, Tuesday)

- Overnight: quiet, healthy. Maintenance digest clean (0 cleanups, 13 links checked, none broken — the link-health classifier from 2026-05-31 is holding; tickettailor false-positives stayed suppressed). Curator had an empty day (0 to inbox — all ~15 candidates filtered as stale-slug/outside-window/no-confirmed-date/hard-reject). Approver disabled by design (no stub — correct). Pending queue: 0.
- Approver work: approved=0 off_topic=0 dup=1 repaired=0 escalated=1. Pending was empty, but the agenda spot-check caught an internal dup: Tuesday at Paradiso showed **two 5Rhythms** — IG-scraped generic "5Rhythms" (Tue 17:00) and curator's "Moving Waves — 5Rhythms w/ Jada Jane Boh & Sophie Daubisse" (Tue 15:00). The official 5rhythms.com registry (MovingWaves-289451) confirms the real class is **Tuesdays 15:00–17:00 at Paradiso** — so Moving Waves is canonical and the IG row's 17:00 is the class END mis-scraped as a start. Archived the generic as dup (migration `20260602030000`). Approved 39→38. Escalated: Resonanz (Thu 18:00) vs Vibración (Thu 19:00) ecstatic dance looked like the same pattern, BUT sources.json `priority_a` already documents Paradiso Thursday as "multiple overlapping series (Vibración, Resonanz, Dance Temple) — treat as a family" → kept both (conservative path was right).
- Feedback: actioned=0 dismissed=0 reviewed=7. **Two users (Mieka, Celina) independently flagged the same gap: informal Contact Improv jams (esp. Nuanu, "whenever Moksa/Dojo isn't on") + water contact are missing.** They handed over 2 Telegram CI groups + 1 WhatsApp zouk group. We already carry ~15 CI events — the gap is the *informal* Nuanu jams, which live in Telegram channels we don't ingest. Added all 3 channels to `curator/sources.json` (CI Telegram → competitor_harvest; zouk → competitor_harvest as borderline-ICP; Nuanu → discovered_pending). Blocked on Benji joining the groups from his phone (same unlock as ShambAllah WhatsApp). Most useful report this week — concrete, ICP-dead-centre, two independent voices.
- **Code fix:** `review-queue.ts` no longer flags missing `start_time` on multi-day all-day events (festivals/markets/art crawls). The maintenance digest had flagged Ubud Open Studios + Writers Festival as "date inconsistencies" every day — false-positive class now killed at source. The festival cards render "Jun 5 – Jun 7" with no time and look correct, so no row edits needed.
- Spot-check: agenda extracted structurally (30 cards) — day-bucketing, recurrence rolling (Moving Waves→Tue, Shamanic Breathwork→Wed, multi-day "Day 2 of 7"), discovery tier all correct. Moving Waves detail = "Tuesday June 2 · 3 PM – 5 PM" (card↔detail parity + registry match). Mobile 390px /events lush, no inversion, no h-scroll, compact hero (Luca's "hero too large" resolved by list-first redesign).
- Site cross-check: all 10 public routes 200.
- Committed: migration `20260602030000` (dup archive); `review-queue.ts` (multi-day start_time guard); `curator/sources.json` (3 user-sourced scouts); removed leftover `scripts/_tmp_fest_covers.ts`; this log. 567 tests pass, build green.
- Followups: (1) **Benji: join the 2 CI Telegram groups + decide on the zouk/Latin WhatsApp** — unlocks the highest-demand ICP gap (CI/Nuanu jams), two users asked same day. (2) ShambAllah WhatsApp invite still outstanding (day 14+). (3) Recurrence-format drift persists (JSON/bare-FREQ/RRULE mix) — one-time normalisation still owed (carryover ×6). (4) 41 low-confidence unresolved venues — batch-resolve on a quiet day.
- Prompt-evolution: yes — see wrap-up (spot-check should consult sources.json venue notes before judging a same-venue/same-day pair a dup).

### 2026-06-02 addendum (~pipeline-revival, Benji: "/events header flashes + nothing's coming in")

- **Header flash:** `events/loading.tsx` rendered the old tall `PageHero` (min-h-[100dvh]) while the page renders the compact `EventsHero` → full-height skeleton collapsed to compact on every load. Fixed: loading.tsx now renders the real `EventsHero` band → zero layout shift. (commit 1885e95)
- **"Nothing's coming in" — diagnosed the pipeline as DEAD, not thin.** `event_sources.last_fetched_at`: Megatix May 27, WhatsApp May 10, Apify May 7, Telegram Mar 11. Root cause: `/api/cron/ingest-events` had **no `maxDuration`** → killed at Vercel Hobby's 10s default mid-Megatix every day → last_fetched_at never advanced. Added `maxDuration=60` + budget 7s→50s + disabled dead WhatsApp/Apify (migration). **But 60s was STILL not enough** — Megatix's 50-event sweep 504'd at 61s even bounded to 20 events (per-event geocode/dedup is the hang). Megatix remains degraded; the durable fix is moving it to a GH-Actions harvester (no timeout), same as below.
- **Telegram is unplugged, not dead:** `getWebhookInfo` → prod webhook URL **unset**, **21 updates queued**. Re-enabled the source (cron skips it; webhook handler needs is_enabled=true). Fix = one `setWebhook` call — flagged for Benji (external state + dumps 21 msgs into LLM pipeline).
- **Phase 2 — ToDo.Today daily harvester SHIPPED & VERIFIED LIVE (the volume win).** ToDo.Today is a JS-rendered WP SPA behind Cloudflare; found its own JSON API (`/api/todo-today/v1/events?channel=ubud`) callable from inside the stealth browser context. New `scripts/scrape/todo-today-harvest.mjs` (API pull + ICP category filter + image + attribution) + `.github/workflows/todo-today-harvest.yml` (daily 02:30 WITA, GH Actions = no timeout) → POST `/api/cron/curator-ingest` (now takes a `source` param). **Live test: 12 ICP events ingested with correct cover images** (DISSOLVE EROS, Kirtan & Cacao, Sound Journey, Clarity/Threefold Breathwork, Thai Massage Jam, Sufi Whirling, 5Rhythms, Bansuri & Sitar) → pending. (commit a979582 + category-map fix)
- **Spider-web vision baked in:** `docs/daily-routine.md` new step 6b (cross-check harvest, don't depend; follow ≥1 sublink/run to promote first-party sources; grow the web each run). `curator/sources.json` ToDo.Today note flipped from "scout, promote when needed" → "LIVE harvested daily" with the API path + the open `TODO(spider-web)` (detail-sublink → ticket URL + organiser-domain promotion).
- Followups: (1) **Megatix → GH-Actions harvester** (Vercel function can't fit a 50-event geocode/dedup sweep in 60s; this is the durable fix + matches the spider-web architecture). (2) **ToDo.Today sublink-following** (capture ticket-direct URL + promote organiser domains — the `TODO(spider-web)`). (3) **Blissbase + Soulwise harvesters** using the ToDo.Today template. (4) **Free custom Instagram scraper** to replace paid Apify (Benji's ask). (5) **Telegram setWebhook** to drain the 21 queued + resume. (6) The 12 todo-today events are pending — next routine triages (several dup our existing recurring rows: 5Rhythms Moving Wave ↔ Moving Waves, DISSOLVE EROS ↔ Dissolve::Eros).
- Note: a **parallel session** was committing pgvector/embeddings work to the same repo during this session (untracked `src/lib/embeddings.ts`, `scripts/embed-events-backfill.ts`, `scripts/cluster-events.ts`, a pgvector migration) — left untouched; committed only my own files explicitly.

### 2026-06-02 addendum 2 (~harvest-web builds, Benji: "continue with the next set")

Executed the next-builds plan. Pending queue went **0 → 76** (Megatix 53, ToDo.Today 12, Telegram 11) — the volume revival, from the durable GH-Actions harvest pattern.

- **Telegram — REVIVED.** Root cause was an unregistered prod webhook (21 updates queued). `telegram-webhook.ts set https://theubudian.life` → drained to 0, webhook live, 11 events created. Source was already re-enabled.
- **Megatix — REVIVED via GH Actions (Tier-A win).** `scripts/scrape/megatix-harvest.mjs` ports the adapter's fetch+map (plain fetch, no browser/deps); targeted ICP search terms (dropped generic "ubud" → killed restaurant/tourist noise) + junk/negative filters → 60 clean ICP events with images + ticket URLs. `aggregator-harvest.yml` (daily 02:40 WITA) POSTs in **chunks of 15** (a 60-event POST 504s the route's 60s geocode budget). CI run GREEN: "megatix ingested 24 (21 dup, 0 failed) of 60." Megatix set `is_enabled=false` (off the timing-out Vercel cron). curator-ingest `ALLOWED_SLUGS` expanded; migration added blissbase/soulwise/instagram-public source rows.
- **Instagram — free POC built + PROVEN, wiring deferred.** `instagram-harvest.mjs`: stealth Chromium + IG `web_profile_info` API → recent posts, event-caption pre-filter. Local: paradiso 7/8 + pyramids 3/8 extracted free. BUT **5/7 handles 404'd even residential** (IG rate-limits; CI datacenter IPs worse) and content is noisy (Paradiso IG = cinema). Committed as a 1-step-enable POC behind a future residential proxy; not worth the LLM-route + workflow for the unreliable/noisy yield now.
- **Deferred (real blockers, documented):** ToDo.Today sublink→ticket-URL (detail pages re-trigger Cloudflare, no detail API — list API has everything but the CTA; daily-routine step 6b handles sublinks manually). Blissbase (`__data.json` is IP-geolocated to the runner = Surabaya, ignores lat/lng → 0 Ubud events; overlaps Megatix/ToDo). Soulwise (500 location-less sitemap UUIDs, yoga-skewed, venue-discovery-only, low yield).
- Followups: (1) **Triage the 76 pending** next routine — many Megatix are dups of existing recurring rows; ToDo has cinema-adjacent edge cases. (2) IG needs a residential proxy to be reliable in CI (or keep Apify disabled-fallback). (3) Megatix harvest steady-state ~10-24 new/day after dedup; today's 53 inflated by manual test POSTs (incl. a 504'd 60-POST that partial-ingested). (4) ToDo.Today sublink-following blocked on a CF bypass for detail pages.
- Commits: 036ee44 (Megatix harvester + wiring), d3692f6 (aggregator-harvest workflow), d346b03 (IG POC). All pushed, CI green.

## 2026-06-03 (~routine + harvest QA, Benji: "run the routine, test the builds, update the prompt, finish the sublink")

- **Overnight: the harvests I built FIRED ON SCHEDULE + succeeded** — todo-today (1m48s) + aggregator/Megatix (4m1s) GH workflows both green. Maintenance digest: auto-archived 9 past pending + cleared 1 stale CTA; flagged 3 broken megatix links + **a Telegram webhook error**. Pending arrived at **85** (megatix 58, todo-today 19, telegram 8).
- **Telegram webhook newline bug — FOUND + FIXED (root cause).** Digest error: `invalid webhook URL` = `https://theubudian.life\n/api/webhooks/telegram`. `telegram-webhook-health.ts` (nightly maintenance self-heal) stripped trailing *slashes* from `NEXT_PUBLIC_SITE_URL` but not the trailing *newline* (the Vercel env gotcha) → re-registered a broken URL every night, clobbering my clean registration. Added `.trim()` to all three env reads. Re-registered the webhook cleanly (drained again).
- **Triaged the 85 → approved 45, archived 40, pending 0.** Approved 38 → **83** (agenda more than doubled). Archived: 7 cross-source dups (FRIDAY Ecstatic Dance/megatix, DISSOLVE EROS ×2 from telegram+todo, 5Rhythms Moving Wave/todo, Authentic Relating Games/telegram, Free Community Entropic/todo, CoCreate Voice Jam/todo — all dup existing recurring rows the fingerprint dedup missed on title variants) + 33 off-ICP (the Megatix "yoga barn"/"sayuri" search pulls the venues' full retail program: face yoga/gua sha/osteopathy/hormone clinics, teacher-trainings/masterclasses, comedy/standup, self-help, language, restaurant music). Migration `20260603030000`.
- **Code-fix-the-pattern: expanded `megatix-harvest.mjs` NEGATIVE filter** to drop that venue-retail/training/beauty/comedy/self-help at the source (it recurs daily). Re-run: 0 noise leaks, 39 dropped vs 13 before.
- **ToDo.Today sublink → ticket URL: EXHAUSTIVELY investigated, HARD-BLOCKED.** List API has no ticket-URL field (dumped every field); 4 detail-API patterns all 403/404; detail pages use a managed/Turnstile Cloudflare challenge the headless stealth shim can't pass even at 45s (vs /ubud/'s auto-solving JS challenge). Needs a paid CF-solver — out of scope. Events flow with images+data, no CTA (playbook-OK). Documented in step 6b + the harvest memory.
- **Updated the routine prompt** (`docs/daily-routine.md`): step 2 now teaches the source-tagged harvest triage (megatix venue-retail archive list, cross-source dup-by-title-variant) + the per-day migration pattern; step 6b sublink note updated to "manual, CF-blocked".
- Followups: (1) **Cross-source dedup by title-variant** — the fingerprint dedup misses "FRIDAY Ecstatic Dance" = "Friday Ecstatic Dance — The Yoga Barn"; a fuzzy title+venue+weekday match at ingest would stop the daily dup-triage. (2) IG still needs a residential proxy. (3) Telegram fix deploys this commit — confirm tomorrow's digest has no webhook error. (4) Agenda is now 83 — watch that /events stays performant + scannable at that density.

### 2026-06-03 addendum (cross-source dedup — followup #1 resolved)

- **Built the cross-source title-variant dedup** (`17a87ac`). Layer 2.5 now derives each recurring event's weekday (rule `day_of_week` or seed-date fallback) and guards on it: same-venue + same-weekday + token-subset title → 0.92 (auto-skip); + partial overlap → 0.78 (→ LLM layer); different weekday → never merges. So "FRIDAY Ecstatic Dance" (megatix) auto-collapses into "Friday Ecstatic Dance — The Yoga Barn" at ingest instead of needing a daily manual archive. +2 tests, 572/572 pass. The recurring daily dup-triage burden should now largely disappear (verify on tomorrow's harvest — fewer dups should reach pending).
