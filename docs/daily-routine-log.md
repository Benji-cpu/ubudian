# Daily routine — log

Each day Claude appends one entry per run of `docs/daily-routine.md`. Newest at the bottom. Use this to spot patterns: same bug class recurring across days, drift in volume, recurring escalations.

Keep entries tight (~10 lines). When the file gets long, archive the oldest months into a yearly file (`daily-routine-log-2026.md`).

> History note: this file was named `docs/morning-prompt-log.md` before 2026-05-27. Entries from before that date still use the old framing ("morning routine") — the content is unchanged.

---

## 2026-05-21 (~60min)

- Overnight: curator surfaced 7 events for 2026-05-21 (SUPERMOON 9th anniv, 2× Kintsugi @ Dragon Tea Temple, Full/New Moon kirtans, Master Lover couples retreat, Heart Space Bali cacao). Maintenance digest flagged 9 missing-`start_time` events + 2 broken links. Approver disabled, queue was sitting unprocessed in `curator/inbox/`.
- Approver work: approved=13, off_topic=10, dup=2, repaired=2, escalated=0. **Root cause for the empty pending queue: GH workflow `curator-ingest.yml` used `ls -t` on a fresh checkout — equal mtimes → unstable sort → it kept POSTing 2026-05-19.json (the oldest file) for three days. Fixed (use `sort | tail -1`) and manually `curl`-POSTed the 2026-05-20 + 2026-05-21 inboxes; both reported 7/7/0/0 ingested.** Digest cleanups: 7× training-not-gathering (Inner Temple ×3, Momentum Contact Facilitator Training ×3, Anandasarita Inner Mastery), 1× dead-link (Divine Darkness "Sunday Worship"), 1× not-specific (Regular Circling), 1× off-brand commercial (Heart Space Bali — confirmed via web fetch, this is destination-wellness tourist-coded, not local ceremony). Dedup: Songs of the Dragonfly ingested twice w/ same Megatix URL (kept long-titled variant), Cremoso Night zouk has Mon+Tue duplicates from conflicting day_of_week parses (kept the one with the WhatsApp join link).
- Spot-check: Card↔detail parity OK on SUPERMOON + Kintsugi Jun 26 (post-rename). No past-date leakage. Title bug surfaced: `/experiences` rendered "Ubud Retreats | The Ubudian | The Ubudian" — root layout uses template `%s | The Ubudian` but ~30 page metadata files redundantly included the suffix. Stripped via sed across `src/app/**/page.tsx`.
- Site cross-check: all 10 main routes 200. `/membership` had no metadata of its own (rendered tab title "The Ubudian"); added one. Yesterday's followup remains: membership tier copy still promises "Humans of Ubud stories" while `stories_enabled=false`.
- Committed: migration `20260521082535_morning_triage_2026_05_21.sql` (14 triage rows + 2 dedup rows); workflow fix in `curator-ingest.yml`; sed pass stripping ` | The Ubudian` from 30+ page metadata files; `/membership` metadata.
- Followups: (1) Promote Dragon Tea Temple to `priority_b` in `curator/sources.json` — 3 of today's approvals came from there; the curator's Sunday Step-11 promotion logic should already catch it but worth a manual nudge. Heart Space Bali should be moved to `rejected_permanent` so it stops re-surfacing. (2) Several long-running approved events have no cover image (Songs of Dragonfly, Solo Improv w/ Nastya, Authentic Relating Games, Thai Massage Jam) — cards fall back to gradient placeholders. (3) Newsletter signup not surfaced on `/events` where intent is highest. (4) The `ls -t` class of bug could exist in the approver-fetch + approver-apply workflows too — audit when they're next active.
- Prompt-evolution: proposed an edit to step 1 of the prompt — see wrap-up.

## 2026-05-20 (~60min)

- Overnight: approver self-healed the BLOCKED stub from 2026-05-19; approved 5 / off_topic 1 / dup 0 / escalated 0. Curator surfaced 7 new events, 4 venues (Dragon Tea Temple, Azadi, Bara Studio, AYMC), 6 facilitators.
- Approver work: pending queue was empty — approver had drained it. My contribution: archived 191 stale `approved` events whose start_date had passed (oldest 2026-03-09), plus 3 dead-link 404s (megatix typo slugs + dead embodiedawakeningacademy URL).
- Spot-check: 8 events (Beauty Way, Full Moon Raio, Friday ED Yoga Barn, Vibración, Dragonfly, Entropic, Contact Dojo Jam, Solo Improv w/ Nastya). Card↔detail parity intact; Entropic shows 11am Wed contact-dance (worth a source check); Vibración price "IDR 90000" no commas. Friday ED megatix slug really is 404 (slug `friday-ecstatic-dance-25` rotates) — cleared the URL.
- Site cross-check: /events + / hero subtitles + CTAs were invisible (charcoal text on dark bg). Root cause: `--brand-cream` and `--brand-deep-green` invert in `.dark`, but those heroes lock their bg dark in both themes. Fixed both heroes with literal hex. /guides, /experiences, /membership healthy. /stories 404 is intentional (`stories_enabled=false`) but membership tier copy still promises "Humans of Ubud stories" — copy bug.
- Committed: migration `20260520070000_archive_stale_approved_events.sql`; `archivePastApprovedEvents()` wired into nightly maintenance; `checkExternalLinkHealth()` uses real browser UA + HEAD→GET fallback; hero text fixes on `events-hero.tsx` + `app/page.tsx`; copy "on right now" → "on the agenda right now".
- Followups: (1) Entropic 11am — sanity-check at source. (2) Membership copy promises stories while stories_enabled=false. (3) 359 usages of `text-brand-cream`/`text-brand-deep-green` — audit any other locked-dark section. (4) Yoga Barn Friday ED needs a per-week URL refresh strategy.
- Prompt-evolution: yes — add a brand-register note about `--brand-cream` inversion on locked-dark heroes (proposed in the wrap-up reply).

## 2026-05-25 (~55min)

- Overnight: maintenance digest healthy (1 archived past pending, 4 broken links, 0 dedup backlog). Curator surfaced 14 candidates → 2 to inbox (Bara Studio Essence of Love + Heart Space Bali Fri Cacao, both already in DB or filtered) and PROMOTED Heart Space Bali to priority_b. Pending queue had 10 events accumulated 2026-05-21..2026-05-23.
- Approver work: approved=2 (Akasha Sat ED, Moving Waves 5Rhythms Tue) + repaired=2 (Pyramids Full Moon ceremony 18:00 + monthly; Ecstatic Dance in the Dark → weekly Wed) + off_topic=8 (4 stale Megatix listings + later rolled back 4 more I'd briefly approved). Cleared the friday-ecstatic-dance-25 megatix URL (slug rotates weekly, dead each time).
- Spot-check: **discovered a systemic curator bug** — it's year-rolling stale Megatix slugs (events that ended in 2021, 2023, 2024, 2025 surfaced as future 2026 dates). 6 events archived for this reason this morning, including SUPERMOON Jun 14 which had been approved since the original ingest (megatix-suara-semesta-001 is the 2025 edition; new editions get -002, etc.). AYNI Jun 14, New Moon Cacao Jun 12 (Levi Banner), Ecstatic Dance in the Dark all same root cause. Added a hard rule to `curator/playbook.md` 2026-05-25 lessons. Also caught a render bug: image-less event cards render the category twice (once in the editorial placeholder typography + once in the Badge chip). Fixed `event-card-placeholder.tsx` to keep just the hairline gold rule.
- Site cross-check: all 12 main routes 200. Live agenda (45 → now 41 approved after rollback) reads clean, "Featured today" pulled correct Monday event (Contact Dojo Jam 6PM at Moksa). Mobile hero shows live count "45 gatherings on the agenda right now" — needs a refresh now that we're at 41; that's a server-render cache issue, will resolve next ISR. New Akasha Sat event surfaces with "NEW" chip.
- Committed: migration `20260525071500_morning_triage_2026_05_25.sql` (10 pending decisions + 1 URL clear); migration `20260525083000_morning_triage_2026_05_25_rollback_stale_megatix.sql` (4 stale-megatix rollbacks); `event-card-placeholder.tsx` declutter; `event-hero.tsx` strip unused `text-6xl`; `curator/playbook.md` 2026-05-25 lesson on stale-Megatix year-rolling.
- Followups: (1) Curator agent doesn't enforce the new playbook rule programmatically — needs prompt update or a pre-inbox filter that does HEAD/GET on Megatix URLs and rejects pages containing "This event has already taken place". (2) Solo Improvisation w/ Nastya has NULL venue_name + free-text recurrence rule "Every Wednesday" — same drift class as the Pyramids of Chi event I just normalised. Worth a sweep: how many approved events have free-text recurrence_rule vs RRULE? (3) Akasha + Moving Waves were approved without external URLs from curator inference — both look credible but unverified; if next week's Akasha doesn't materialise, archive. (4) AYMC has 2 qualifying curator logs already; one more away from priority_a promotion. Worth surfacing the Sayan/Alchemy Rise Ecstatic Dance Tuesday 7-9pm as a manual add.
- Prompt-evolution: yes — one-line addition to "The work" step 2, see wrap-up reply.

## 2026-05-27 (~40min)

- Overnight: quiet run — maintenance digest clean (0 cleanups, 2 failed messages purged, 1 broken link: Bara Studio tickettailor 403 = same Cloudflare false-positive class flagged yesterday). Curator surfaced 8 candidates → 0 to inbox (all already-ingested weeklies, geo-out, or 403-blocked). Pending queue genuinely empty (verified via DB).
- Approver work: approved=0 off_topic=0 dup=0 repaired=0 escalated=0 (nothing in pending).
- Spot-check: 5 events (Authentic Relating Games, Friday ED Yoga Barn, Master Lover Retreat, Ilahi Sufi Qawwali, Dance of the Dragonfly Sunset). 4/5 card↔detail parity intact. **1/5 mis-dated: Friday Ecstatic Dance — The Yoga Barn renders "Saturday May 30" everywhere (card + detail + agenda).** Root cause: `nextOccurrence()` in `buckets.ts` (and mirror in `recurrence.ts` `expandRecurrence`) only ran the day-walker for `Array.isArray(rule.day_of_week)` — single-number `day_of_week` fell through to a `start_date + 7*N` stride. Friday ED has `recurrence_rule="FREQ=WEEKLY;BYDAY=FR"` parsed to `day_of_week:5` (number) but `start_date=2026-05-23` (Saturday) → stride landed on Saturdays. Same class affects future-anchored row "Dance of the Dragonfly Expanded" (rule WE, seed Thu Jul 23) once that date arrives. Fix: day-walker now runs for any weekly rule with `day_of_week` defined (number or array), anchored at `max(today, start_date)`. 548/548 unit tests pass; `npm run build` green.
- Site cross-check: 8 main routes 200 (/events, /, /experiences, /membership, /quiz, /guides, /about, /partners). Membership copy no longer references "Humans of Ubud / Stories" — yesterday's followup #2 is resolved. Mobile 390px /events hero renders cleanly (lush, readable, brand-tonal). 2× `<h1>` on detail pages persists (known E2E failure per MEMORY.md). Home "What's Happening" features Moving Waves (Tue) + Saturday ED + Full Moon Blue Moon Cacao + Master Lover — looks editorially curated rather than chronologically-today-first; intentional but worth a re-check (with my fix, Moving Waves now rolls to Tue Jun 2 — confirm).
- Discoveries: curator added Akari Tamura (Anandamoyi Dasi) as facilitator-pending (Dragon Tea Temple Wed Kirtan bi-weekly). 6th consecutive 403 on PURNAMA Megatix — full moon dates corrected to Jun 29 / Jul 29 in sources.json. AYMC Full Moon Bhakti Aug 8 surfaced as next confirmed.
- Committed: `<TBD>` fix(events): weekly recurrence day-walker honours single-number day_of_week — repairs Friday ED Saturday-misdating and any future row whose seed weekday disagrees with rule.
- Followups: (1) **link-health checker still false-positives on Cloudflare** — Bara Studio tickettailor 403 in today's digest is identical pattern to yesterday's, will keep landing daily until the checker differentiates `cf-mitigated` / "Just a moment..." from genuine 404. Highest-priority infra fix. (2) Three recurrence-rule formats still coexist (14 JSON, 9 RRULE, 2 normalised); a one-time migration to one canonical shape (JSON) would simplify the parser surface. (3) Home page "What's Happening in Ubud" featured-event ranking doesn't surface today's events at all — investigate whether by design or stale. (4) Curator's `playbook.md` stale-Megatix-detection rule is still not enforced programmatically (carried over from 2026-05-25).
- Prompt-evolution: yes — one-line addition to step 4 (spot-check), see wrap-up.

## 2026-05-28 (~50min)

- Overnight: maintenance digest clean (0 cleanups, 1 false-positive 403 = Bara Studio Cloudflare class persisting daily). Curator surfaced 8 candidates → 0 to inbox in the daily run (all weeklies / 403-blocked) BUT the same day's competitor-harvest sweep added 3 inbox items (Five Temples of Her, Shamanic Breathwork with Jane, Shakti Bhakti Kirtan). Pending queue had 14 events accumulated (megatix 12 + curator 2).
- Approver work: approved=6 off_topic=7 dup=1 repaired=4 (start_time backfills from description quotes) + 2 RESURRECTED archived-recurring rows (Shamanic Breathwork @ Yoga Barn Wed; Shakti Bhakti Kirtan @ Intuitive Flow Wed). **Discovered a code bug**: `archivePastPendingEvents()` in `src/lib/ingestion/alerts.ts` archives ANY pending row with `start_date < today`, missing the `is_recurring=false` guard that its sibling `archivePastApprovedEvents()` has. When the competitor-harvest sweep landed recurring rows with start_date=today's-UTC-date as pending, the next maintenance cron wrongly archived them. One-line fix shipped: `.eq("is_recurring", false)` added. 548/548 tests still pass.
- Feedback: actioned=0 dismissed=0 reviewed=1 — Russ (russty75@gmail.com) on /quiz: praises quiz UX, asks to list his Wed-eve Authentic Relating event. The existing approved AR Games row is hosted by Ram, so Russ runs a different Wed AR series — flagged for Benji to reply with trusted-submitter pathway + intake fields. Also: Russ suggests a "healing" archetype for the quiz — quiz-v2 candidate.
- Spot-check: 6 newly-approved detail pages all 200; 5 Megatix external links all 200; Shamanic Breathwork detail page renders correctly with day-walker rolling May 27 (Wed seed) → June 3 (next Wed) — recurrence fix from 2026-05-27 still holding. Card↔detail parity intact. Hero count "49 gatherings on the agenda right now" matches DB.
- Site cross-check: all 10 main public routes 200. Mobile 390px /events hero renders lush + brand-tonal (no `text-brand-cream` inversion regressions). Detail page cross-link section "More Ceremony & Sound Events" working — Shamanic Breathwork detail shows related kirtan/sound events. /stories returns 200 (placeholder rendering, stories_enabled=false).
- Discoveries: curator log surfaced 3 venues (ASH at Nuanu, La Portal to Shamballah, Intuitive Flow Yogastudio) and 2 facilitators (Mariama Del Mar, Anu Karoliina) on yesterday's competitor-harvest. Intuitive Flow weekly Wed kirtan is now live (resurrected); ASH at Nuanu still 1-of-3 qualifying logs. Akari Tamura (Dragon Tea Temple bi-weekly Wed Kirtan) added to facilitators_pending — track for Megatix unblock.
- Committed: `<TBD>` migration `20260528083000_daily_routine_2026_05_28.sql` (14 pending decisions + 2 resurrected recurring); `src/lib/ingestion/alerts.ts` one-line `is_recurring=false` guard; daily-routine-log entry. Single commit, single push.
- Followups: (1) **Artifact filing convention is UTC-dated, not WITA** — the daily-routine prompt's `$(TZ=Asia/Makassar date +%F)` looks for files dated 2026-05-28 when the actual artifacts land under 2026-05-27 (UTC at trigger fire time = 19:17 UTC May 27 = 03:17 WITA May 28). I figured it out from commit message + log timestamps but a future run might waste 5 minutes on this. **Prompt-evolution proposal below**. (2) Cloudflare false-positive on Bara Studio tickettailor 403 persists (carried from 2026-05-27, 2026-05-26 — 3 consecutive days). Highest-priority infra fix: differentiate `cf-mitigated` / "Just a moment..." from genuine 404 in `checkExternalLinkHealth`. (3) Three recurrence-rule formats still coexist in the DB (JSON, RRULE, freetext) — carryover. (4) The competitor-harvest sweep landing 3 useful events (vs. daily curator's 0) suggests Blissbase + Soulwise scouting is now the higher-value path on quiet days; consider promoting that walk to a daily slot, not just on-demand.
- Prompt-evolution: yes — see wrap-up.

## 2026-05-26 (~55min)

- Overnight: maintenance digest clean (0 cleanups, 2 broken-link 403s — both Cloudflare bot-block false-positives, NOT genuine deadness). Curator surfaced 3 candidates (Ilahi Sufi Qawwali Yoga Barn Jun 17, Heart Space Bali Sun cacao, Pyramids of Chi Full Moon Jun 29) → 2 dedup'd against existing rows → 1 landed in pending. Discovered Om Ham Retreat + Ilahi Sufi + Sawitri facilitators.
- Approver work: approved=1 (Ilahi Sufi Qawwali, Megatix URL verified 200) + archived=2 (Inner Temple Level 2 + Alchemy & Qigong Intensives — multi-day intensives that escaped `archivePastApprovedEvents` because `end_date>=today` for the longer one) + repaired=3 (Contact Dojo Jam Monday recurrence corruption + 2 freetext recurrence normalisations) + url_cleared=1 (megatix `ecstantric-dance` genuinely 403 even in browser).
- Spot-check: **discovered a render bug on the hero card.** "Featured today" badge was hard-coded; hero = ranked[0] regardless of when it happened. Yesterday (Mon) it surfaced Contact Dojo Jam Mon — coincidentally correct. Today (Tue) it surfaced Contact Dojo Jam with date "Wednesday, May 27" (next M/W/F instance via corrupted recurrence rule) under the same "Featured today" badge — triple lie: label/date/description all disagreed. Fixed at code: hero now walks bucketEventsByTime nearest-to-furthest, picks top-ranked event within first non-empty bucket, label adapts (Happening now / Featured today / Featured tomorrow / Featured this weekend / Featured this week / Coming up). Card↔detail parity verified across 4 events (Moving Waves, Contact Dojo Mon, Authentic Relating, Pyramids of Chi) — all match. Recurrence-rule schema audit: 14 JSON + 9 RRULE + 2 freetext approved recurring rows. Normalised the 2 freetext rows (Solo Improv w/ Nastya, Authentic Relating Games) to JSON; 9 RRULE rows still work via tolerant parser but should be normalised in a future migration.
- Site cross-check: all 10 main public routes 200. `/journeys` 404 but no nav points to it — false alarm. Mobile + desktop hero render verified post-deploy.
- Committed: `e8f00b2` fix(events): hero anchors to today's bucket, label adapts; SQL-only triage (approve Ilahi, archive 2 intensives, fix Contact Dojo Mon recurrence to [1], clear ecstantric URL, normalise 2 freetext rules) — direct UPDATEs, no migration since <10 rows.
- Followups: (1) **link-health checker false-positives on Cloudflare bot-blocked hosts** — tickettailor.com always 403 to UA-spoofed HEAD but the page is fine; megatix.co.id mostly works (200 on Ilahi, 403 on dead slugs). The check needs to differentiate genuine 404 from Cloudflare challenge (look for `cf-mitigated` header or "Just a moment..." in body) before flagging. Same noise will keep landing in the digest every day until fixed. (2) Three recurrence-rule formats coexist — write a one-time migration to normalise all 9 RRULE rows to JSON so the schema has one canonical shape. (3) `archivePastApprovedEvents` missed Inner Temple Level 2 (end_date 2026-05-25 < today 2026-05-26) — the SQL filter looks correct, may be a `nowInBali` edge or the cron didn't run; worth instrumenting one log line. (4) Yesterday's curator-playbook stale-Megatix rule is still not enforced programmatically — Ilahi link was 200 today, but the next Megatix import may year-roll something dead again.
- Prompt-evolution: yes — one-line addition to step 8, see wrap-up reply.
