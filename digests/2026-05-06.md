# Daily maintenance — 2026-05-06

## Status: BLOCKED

The nightly-routine agent could not reach `/api/cron/daily-maintenance` on either deployment URL:

- `https://ubudian.vercel.app/api/cron/daily-maintenance?digest=true` → HTTP 403
- `https://ubudian-v1.vercel.app/api/cron/daily-maintenance?digest=true` → HTTP 403

**Error body:** `Host not in allowlist`

This response comes from Vercel's edge layer before the route is reached. The agent's outbound IP is not permitted by Vercel's Deployment Protection / IP allowlist for this project.

## Action required

To unblock future runs, one of:

1. **Disable Deployment Protection** for the Vercel project (if it's enabled under Project Settings → Deployment Protection), or
2. **Add the Anthropic Claude Code agent egress IP range** to the allowlist, or
3. **Bypass protection for the cron path** using a Vercel firewall rule that exempts `/api/cron/*` when the `Authorization: Bearer <CRON_SECRET>` header is present.

No autonomous cleanups were performed. No review queue was assembled. No digest email was sent.
