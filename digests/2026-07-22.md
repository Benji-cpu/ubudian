# Daily maintenance — 2026-07-22

**Status: BLOCKED — payload missing (self-heal failed)**

`digests/2026-07-22.json` was not present when this agent fired at ~19:17 UTC. After waiting 60s and retrying `git pull`, the file was still absent. Self-heal workflow dispatch was attempted but failed:

- `gh` CLI not available in the sandbox
- GitHub MCP `actions_run_trigger` returned 403 ("Resource not accessible by integration")

**Root cause (observed pattern):** The `daily-maintenance-fetch` GH workflow is scheduled at 19:02 UTC but has consistently drifted to ~20:06–20:26 UTC over the past several days (runs visible at https://github.com/Benji-cpu/ubudian/actions/workflows/daily-maintenance-fetch.yml). This agent fires at 19:17 UTC — only 15 min after the scheduled time, but ~50–70 min before actual execution. The 15-minute scheduling gap is insufficient for the observed drift.

**The JSON will likely land on main around 20:10–20:30 UTC today** (based on prior run times), but no agent will synthesise the markdown after that point.

**Recommended fix:** Increase the scheduling gap — move this Claude trigger from 19:17 UTC to at least 20:45 UTC, or adjust the GH workflow cron to run reliably at 19:02 UTC (GitHub cron pile-up at round hours is a known issue; off-minute scheduling helps but doesn't eliminate drift on congested runners).

No autonomous cleanups verified, no review queue assembled by this agent.
