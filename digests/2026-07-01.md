# Daily maintenance — 2026-07-01

**Status: BLOCKED — payload missing (self-heal failed)**

The GitHub Actions workflow `daily-maintenance-fetch` did not produce `digests/2026-07-01.json` before this agent ran, and the self-heal dispatch also failed.

**Root cause diagnosed:** GH cron drifted — yesterday's run (`28397885348`) was created at 19:39 UTC (37 min late vs the 19:02 schedule), while this agent fires at 19:17 UTC. The workflow simply hadn't started yet. The 15-minute scheduling slack is insufficient when GH cron drifts >15 min.

**Self-heal failure:** Both dispatch attempts returned 403. The seeded `GITHUB_PAT` lacks `workflow` scope — it can read/write repo contents but cannot trigger `workflow_dispatch` events. The GitHub MCP server similarly returned 403.

**Actions needed:**
1. Regenerate the `GITHUB_PAT` with the **Workflows** permission enabled, then update the trigger's seed context via `RemoteTrigger { action: "update", trigger_id: "trig_01CnuNJSs8m8wdVyeVrDHrKq" }`.
2. Consider pushing the agent trigger from `19:17 UTC` to `20:00 UTC` (or later) to absorb GH cron drift reliably — yesterday's run completed around 19:40 UTC.

Check run history at: https://github.com/Benji-cpu/ubudian/actions/workflows/daily-maintenance-fetch.yml
