# Daily maintenance — 2026-07-21

**Status: BLOCKED — payload missing (self-heal failed)**

The GitHub Actions workflow `daily-maintenance-fetch` had not produced `digests/2026-07-21.json` by the time this agent ran at 19:17 UTC.

**Root cause — systematic timing mismatch:** The workflow is scheduled at `2 19 * * *` UTC but consistently runs ~1h late (historical runs: 20:06–20:38 UTC). The nightly-routine trigger fires at 19:17 UTC, which is 49–81 minutes *before* the workflow typically completes. Self-heal via `gh workflow run` was attempted but failed (no `gh` binary installed; GitHub MCP returned 403 for `workflow_dispatch`).

**Recommended fix:** Shift the nightly-routine trigger to `30 20 * * *` UTC (≈04:30 WITA) to fire reliably after the GH workflow window. Update via:
```
RemoteTrigger { action: "update", trigger_id: "trig_01CnuNJSs8m8wdVyeVrDHrKq", body: { cron_expression: "30 20 * * *" } }
```

**Catch-up:** `digests/2026-07-20.md` was synthesised in this same run from the existing `digests/2026-07-20.json` (committed by GH workflow at 20:09 UTC on 2026-07-19).

No autonomous cleanups verified, no review queue assembled for 2026-07-21.
