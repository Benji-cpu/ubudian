# Daily maintenance — 2026-06-21

**Status: BLOCKED — payload missing (self-heal failed)**

The GitHub Actions workflow `daily-maintenance-fetch` did not produce `digests/2026-06-21.json` before this agent ran (19:17 UTC). As of 19:18 UTC the workflow had no queued, in-progress, or completed runs since 2026-06-19T19:39Z — GH cron has drifted again.

Self-heal dispatch via GitHub MCP (`actions_run_trigger`) was attempted but returned HTTP 403 (integration lacks `workflow` write scope). The `gh` CLI is not available in this sandbox. The Vercel route cannot be reached directly from the Anthropic sandbox egress allowlist.

**Next steps for the human:**
1. Manually trigger the workflow: https://github.com/Benji-cpu/ubudian/actions/workflows/daily-maintenance-fetch.yml → "Run workflow"
2. Or verify `CRON_SECRET` is still in sync between Vercel and the GH repo secret (a mismatch causes a 401 inside the workflow, aborting before any commit).
3. To prevent future drift: consider adding a `workflow_dispatch` trigger from a more reliable external scheduler, or bump the GH MCP token scope to include `workflow` so self-heal works next time.

No autonomous cleanups verified, no review queue assembled by this agent.
