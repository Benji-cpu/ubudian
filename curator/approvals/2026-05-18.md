# Event approvals — 2026-05-18

**Queue size:** unknown — Supabase MCP unreachable from this trigger's sandbox (no decisions applied).

## Needs Benji
- Supabase MCP OAuth bounced with `HTTP 403: Host not in allowlist` when this agent tried to authenticate from the remote trigger environment. The approver has the `mcp__supabase__execute_sql` permission in its tool list, but the underlying MCP host (`mcp.supabase.com`) is not reachable from the Anthropic sandbox without an interactive `/mcp` auth step that a scheduled trigger cannot perform.
- Net effect: the pending queue was not walked today. Every ingested event from the Telegram / WhatsApp / Eventbrite / curator pipelines remains in `status='pending'` until either (a) Benji approves them manually in `/admin/events`, or (b) this trigger is rewired to use the git-as-bus pattern the `daily-maintenance` and `daily-curator` triggers already use (GH Actions pulls the queue → commits JSON to `main` → trigger reads JSON, decides, commits SQL UPDATEs or a decisions JSON for a follow-up Vercel route to apply).

## Notes
- First run of this agent — no prior approvals/ entries to compare against, so this is the baseline.
- Recommended fix mirrors the architecture documented in `CLAUDE.md` → *Daily Curator Trigger* and *Trigger Maintenance*: introduce `.github/workflows/approver-fetch.yml` to dump `events WHERE status='pending' AND source_id IS NOT NULL` to `curator/approvals/inbox/${TODAY}.json` on `main`, then have this agent read that JSON, decide, and commit a `curator/approvals/decisions/${TODAY}.json` that a new `/api/cron/approver-apply` Vercel route consumes (CRON_SECRET bearer, applies the UPDATEs). Same git-as-bus pattern that already works for the maintenance digest and the curator inbox.
- Until that's wired, the in-flight Gemini gate is gone (per the 2026-05-18 events overhaul) but no replacement editorial gate is firing → pending events accumulate. Lower-risk than the old auto-approve behaviour (no tourist-trap content leaks to `/events`), but the `/admin/events` queue will need manual attention.
